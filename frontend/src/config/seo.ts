import { SITE_CONFIG } from './site';
export const DEFAULT_SEO = { title: SITE_CONFIG.name, titleTemplate: `%s | ${SITE_CONFIG.name}`, description: SITE_CONFIG.description, keywords: ['portfolio', 'developer', 'react'] };
